import { Card, CardContent } from "@/components/ui/card";

export function FeedbackTab() {
  return (
    <Card>
      <CardContent className="text-center py-8">
        <p className="text-muted-foreground">Feedback interface will be implemented here.</p>
      </CardContent>
    </Card>
  );
}